Regenerate TypeScript types from the OpenAPI spec:
1. Run `npm run gen-types` to fetch the latest OpenAPI schema and generate types
2. Verify the generated files in src/generated/
3. Run `npm run build` to ensure generated types compile
4. Run `npm test` to ensure nothing is broken
